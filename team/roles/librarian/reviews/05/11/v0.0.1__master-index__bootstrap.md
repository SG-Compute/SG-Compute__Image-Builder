# Librarian review — master-index bootstrap

**Version:** v0.0.1
**Date:** 2026-05-11

## What landed

- `team/roles/librarian/master-index.md` populated with current briefs / reviews
- Per-role README templates installed for all six roles
- Date-folder convention `MM/DD/` (no year) applied per dev pack §11
- `humans/dinis_cruz/feedback-to-sg-compute.md` seeded with two anticipated items (sg-compute `--json` flags for `lc list` and `lc exec`)

## What's NOT here

- `scripts/lint_briefs.py` (naming-convention lint) — deferred to a follow-up; the convention is documented but not yet machine-enforced
- 90-day archive cron — no items to archive yet
- Cross-link verifier — deferred; few links exist today

## Naming convention reminder

```
v<version>__<type>__<topic-slug>.md
```

Brief types:

| Type             | Example                                                  |
|------------------|----------------------------------------------------------|
| `brief`          | `v0.0.1__brief__m0-bootstrap.md`                         |
| `arch-brief`     | `v0.1.0__arch-brief__strip-minimal.md`                   |
| `dev-brief`      | `v0.1.0__dev-brief__bundle-publisher.md`                 |
| `research-brief` | `v0.1.0__research-brief__zstd-vs-gzip-payloads.md`       |
| `technical-brief`| `v0.1.0__technical-brief__ec2-ephemeral-launcher.md`     |
| `decision`       | `v0.1.0__decision__ssh-over-ssm.md`                      |
| `debrief`        | `v0.0.1__debrief__m0-bootstrap.md`                       |

## Next

Update this index after each merge into `dev` or `main`.
