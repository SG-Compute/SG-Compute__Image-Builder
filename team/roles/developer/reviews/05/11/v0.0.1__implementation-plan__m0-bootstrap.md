# Developer review — M0 bootstrap implementation plan

**Version:** v0.0.1
**Date:** 2026-05-11
**Brief:** `humans/dinis_cruz/briefs/05/11/v0.0.1__brief__m0-bootstrap.md`
**Pack milestone:** M0 (per `09__implementation-plan/implementation-plan.md` in the dev pack)

## What "M0 done" means here

Acceptance per pack §09 M0:

> Anyone can clone the repo, `pip install -e .`, and run `sgi --version`.

Plus the hard boundary from the HANDOVER: **stop after M0** and confirm
spike status before touching M1.

## Concrete tasks (mapped to pack §09 M0)

| #   | Pack task                                            | Status |
|-----|------------------------------------------------------|--------|
| 0.1 | Repo at target URL                                   | done — branch `claude/confirm-repo-access-4CFac` pushed |
| 0.2 | Directory structure per architecture                 | done — full tree under `sg_image_builder/`, mirror under `sg_image_builder__tests/`, 9 specs scaffolded |
| 0.3 | `pyproject.toml` with required deps                  | done — osbot-utils, osbot-aws, typer, rich, zstandard; ruff/mypy/pytest configured |
| 0.4 | `LICENSE`, `README.md`, `version` = `v0.0.1`         | done — Apache 2.0 kept; README rewritten; `sg_image_builder/version` file added |
| 0.5 | `humans/` + `team/roles/` skeleton                   | done — 6 role READMEs; date-folder convention (`05/11/`) per pack |
| 0.6 | CI pipeline (per pack §10)                           | done — `pr.yml`, `main.yml`, `tag.yml` |
| 0.7 | `sgi --version` works                                | done — typer-based `app.py` with `--version` flag and `version` subcommand |

## Decisions taken

- **Branch model:** `main + dev` per pack §10 (HANDOVER Q5).
- **Spec scaffolding:** README-only stubs for all 9 specs in M0; real content lands in M5/M9/M10.
- **`version` file format:** holds the human/git-tag form (`v0.0.1`); `sg_image_builder.__version__` strips the leading `v` so PEP 440 / setuptools dynamic version is happy.
- **CI E2E:** workflow `tag.yml` is present but its E2E job will be a no-op until `SGI_E2E_ENABLED=1` plus AWS secrets are configured. No AWS costs until then.

## Stop boundary observed

No M1 code written:

- `Storage` interface present as an empty `__init__.py` directory, no abstract class yet
- `Exec_Provider` likewise
- Spike code in `SGraph-AI__Service__Playwright/sg_image_builder/` has NOT been lifted yet — that's PR 2 after spike status confirmed

## Follow-ups for the human before M1

1. Confirm `team/comms/briefs/v0.2.8__sgi-spike-results.md` in the sister repo - is the spike done? Strong/conditional/fail?
2. Confirm v0.1.0 spec scope (all 9 or a subset?).
3. Confirm whether `Exec_Provider__Sg_Compute` ships in v0.1.0 at all (HANDOVER Q4).
4. Branch protection rules on `main` and `dev` need to be applied in the GitHub UI (Settings -> Branches) - the CI workflow gives them their required-status-check names.

## Next milestone hand-off

M1 (Provider foundations). Starting point is the **lift** described in the
HANDOVER §"What you start with" — copy from sister-repo's `sg_image_builder/`
once spike outcome is confirmed.
